info:
  name: Confirm Account
  type: http
  seq: 2

http:
  method: PUT
  url: "{{base_url}}/api/users/confirm-account/{{confirm_guid}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
