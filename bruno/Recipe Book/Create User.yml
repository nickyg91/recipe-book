info:
  name: Create User
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5266/api/users/sign-up
  body:
    type: json
    data: |-
      {
        "username": "Biggymac",
        "password": "Test12#$",
        "email": "test@test.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
