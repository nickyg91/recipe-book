info:
  name: Get Logged In User
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/users/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
