info:
  name: Refresh Token
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/api/users/token/{{refresh_token}}/refresh"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
