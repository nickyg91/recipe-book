info:
  name: Generate JWT
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/api/users/log-in"
  body:
    type: json
    data: |-
      {
        "email": "test@test.com",
        "password": "Test12#$"
      }

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        bru.setEnvVar("access_token", data.token);
        bru.setEnvVar("refresh_token", data.refreshToken)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
