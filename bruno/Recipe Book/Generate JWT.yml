info:
  name: Generate JWT
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:5266/api/users/log-in
  body:
    type: json
    data: |-
      {
        "email": "test@test.com",
        "password": "Test12#$"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
